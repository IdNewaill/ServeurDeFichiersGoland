# Documentation for clients

## Defaults commands
Here are the commun commands that are accessibles to you (locals commands).

- > end
    
    Close the communication with the server.

## Standarts Commands
Here are the commun commands that a server **can possibly have** but is not forced to.

- > end

    If your server connector app doesn't have the end command, you can probably still find that command in the server you are connected to !

- > help

    Give you all the commands you can have access to.

